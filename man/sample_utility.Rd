% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{sample_utility}
\alias{sample_utility}
\title{Metropolis-Hastings update of the latent utilities}
\usage{
sample_utility(
  constraints,
  zeta,
  Y,
  U,
  active,
  Mu,
  kernel = "exponential",
  control = crr_control(),
  block = NULL
)
}
\arguments{
\item{constraints}{A \link{crr_constraints} object.}

\item{zeta}{Latent utility matrix (\code{n} x \code{d}).}

\item{Y}{Response matrix (\code{n} x \code{d}).}

\item{U}{Current dual matrix (\code{n} x \code{m}); must be feasible.}

\item{active}{Active-constraint mask from \code{\link[=init_dual]{init_dual()}}.}

\item{Mu}{Mean matrix (\code{n} x \code{d}), e.g. \code{X \%*\% beta}.}

\item{kernel}{\code{"exponential"} or \code{"half_gaussian"}.}

\item{control}{A \link{crr_control} object.}

\item{block}{Number of coordinates to update this step. When \code{NULL}
(default), it is resolved from \code{control$zeta_block}: the fixed integer if
set, otherwise \code{min(d, 100)} (the adaptive controller in \code{\link[=crr]{crr()}} supplies
an explicit value each step, so this fallback only applies to direct
callers under an adaptive control).}
}
\value{
List with \code{zeta} (updated matrix) and \code{accept} (0/1 vector,
per-observation acceptance).
}
\description{
One MH step of \eqn{\zeta \mid U, \beta, y}: proposes new utilities for a
random block of coordinates from the truncated-normal proposal, forms the
coordinatewise envelope \eqn{\tilde\zeta} (max where \eqn{y=1}, min where
\eqn{y=0}), runs hit-and-run under \eqn{\tilde\zeta} to obtain a dual
certificate \eqn{U^\star}, and accepts each observation's proposal iff its
\eqn{u_i^\star} is feasible for the current \eqn{\zeta_i}.
}
\keyword{internal}
