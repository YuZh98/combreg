% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_crr}
\alias{simulate_crr}
\title{Simulate combinatorial response regression data}
\usage{
simulate_crr(n, p, constraints, beta = NULL, seed = NULL)
}
\arguments{
\item{n}{Number of observations.}

\item{p}{Number of covariates.}

\item{constraints}{A \link{crr_constraints} object defining \eqn{\mathcal{Y}}.}

\item{beta}{Optional true coefficient matrix (\code{p} x \code{d}).}

\item{seed}{Optional integer seed.}
}
\value{
List with \code{Y} (\code{n} x \code{d}), \code{X} (\code{n} x \code{p}), \code{beta} (\code{p} x \code{d},
the truth), \code{zeta} (\code{n} x \code{d}), and \code{constraints}.
}
\description{
Draws \eqn{X_{ik} \sim N(0,1)}, coefficients
\eqn{\beta_{kj} \sim N(0,1)} (unless supplied), latent utilities
\eqn{\zeta_i = \beta^\top x_i + \varepsilon_i} with standard normal noise,
and responses \eqn{y_i = \arg\max_{y \in \mathcal{Y}} \zeta_i^\top y}
solved by integer programming.
}
\examples{
con <- crr_constraints(rbind(c(1, 1, 0), c(0, 1, 1)), b = c(1, 1))
sim <- simulate_crr(n = 20, p = 2, constraints = con, seed = 1)

}
