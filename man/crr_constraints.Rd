% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{crr_constraints}
\alias{crr_constraints}
\title{Constraint system for combinatorial responses}
\usage{
crr_constraints(A, b, direction = "<=", check_tum = TRUE)
}
\arguments{
\item{A}{Integer constraint matrix (\code{m} x \code{d}).}

\item{b}{Integer right-hand side vector of length \code{m}.}

\item{direction}{Either a single string or a length-\code{m} character vector
with entries \code{"<="} or \code{">="}. Internally all constraints are stored in
\code{"<="} form.}

\item{check_tum}{Verify total unimodularity of \code{A} (exhaustive check,
exponential in \code{min(m, d)}). Set to \code{FALSE} for large matrices whose TUM
structure is known, e.g. network/incidence matrices.}
}
\value{
An object of class \code{crr_constraints} with elements \code{A}, \code{b}
(in \code{<=} form), \code{m}, \code{d}.
}
\description{
Defines the feasible set \eqn{\{y \in \{0,1\}^d : Ay \le b\}} of a
combinatorial response. The matrix \code{A} must be integer-valued and, for the
exactness of the dual-certificate augmentation used by \code{\link[=crr]{crr()}}, totally
unimodular.
}
\examples{
A <- rbind(c(1, 1, 0), c(0, 1, 1))
con <- crr_constraints(A, b = c(1, 1))

}
