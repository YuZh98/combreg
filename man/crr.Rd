% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crr.R
\name{crr}
\alias{crr}
\title{Bayesian combinatorial response regression}
\usage{
crr(
  Y,
  X,
  constraints,
  data = NULL,
  method = c("mhwg", "unconstrained"),
  kernel = c("exponential", "half_gaussian"),
  prior = crr_prior(),
  n_iter = 5000,
  warmup = floor(n_iter/2),
  thin = 1,
  chains = 1,
  seed = NULL,
  control = crr_control(),
  verbose = FALSE
)
}
\arguments{
\item{Y}{Response matrix (\code{n} x \code{d}), rows in \eqn{\mathcal{Y}}.}

\item{X}{Covariate matrix (\code{n} x \code{p}), or a one-sided formula (e.g.
\code{~ x1 + x2}) evaluated against \code{data} via \code{\link[=model.matrix]{model.matrix()}} to build the
design matrix (with the usual factor expansion, interactions, and
intercept; write \code{~ 0 + ...} to drop the intercept).}

\item{constraints}{A \link{crr_constraints} object. Ignored (with a warning)
for \code{method = "unconstrained"}.}

\item{data}{Data frame used to evaluate \code{X} when it is a formula; ignored
when \code{X} is already a matrix.}

\item{method}{Sampler: \code{"mhwg"} (the paper's method) or
\code{"unconstrained"} (baseline).}

\item{kernel}{Dual kernel for \code{"mhwg"}: \code{"exponential"} or
\code{"half_gaussian"}.}

\item{prior}{A \link{crr_prior} object.}

\item{n_iter}{Total MCMC iterations per chain (including warmup).}

\item{warmup}{Warmup iterations discarded by \code{summary()}, \code{plot()}, and
the \code{as.mcmc}/\code{as_draws} converters. All draws are stored.}

\item{thin}{Thinning applied by the converters (draws are stored unthinned).}

\item{chains}{Number of independent chains, run serially.}

\item{seed}{Optional integer seed (\code{set.seed()} is called if supplied).}

\item{control}{A \link{crr_control} object.}

\item{verbose}{Print progress every 1000 iterations.}
}
\value{
An object of class \code{crr_fit}; see \code{\link[=summary.crr_fit]{summary.crr_fit()}}.
}
\description{
Fits the latent-utility regression model
\deqn{\zeta_i = \beta^\top x_i + \varepsilon_i, \quad
      \varepsilon_i \sim N(0, I_d), \quad
      y_i = \arg\max_{y \in \mathcal{Y}} \zeta_i^\top y,}
where \eqn{\mathcal{Y} = \{y \in \{0,1\}^d : Ay \le b\}} with totally
unimodular \code{A}, by MH-within-Gibbs sampling on the dual-certificate
augmented posterior (\code{method = "mhwg"}). \code{method = "unconstrained"} fits
independent Albert-Chib probit regressions per coordinate, ignoring the
constraints --- useful as a baseline demonstrating constraint-ignoring bias.
}
\examples{
if (requireNamespace("lpSolve", quietly = TRUE)) {
  con <- crr_constraints(rbind(c(1, 1, 0), c(0, 1, 1)), b = c(1, 1))
  sim <- simulate_crr(n = 50, p = 2, constraints = con, seed = 1)
  fit <- crr(sim$Y, sim$X, con, n_iter = 200, warmup = 100,
             chains = 1, seed = 1)
  summary(fit)
}

}
