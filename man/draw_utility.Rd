% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{draw_utility}
\alias{draw_utility}
\title{Draw latent utilities from their truncated-normal full conditional}
\usage{
draw_utility(zeta, Mu, Y, UA, subset = seq_len(ncol(zeta)))
}
\arguments{
\item{zeta}{Current latent utility matrix (\code{n} x \code{d}); returned unchanged
outside \code{subset}.}

\item{Mu}{Mean matrix (\code{n} x \code{d}), e.g. \code{X \%*\% beta}.}

\item{Y}{Response matrix (\code{n} x \code{d}).}

\item{UA}{Dual bounds \code{U \%*\% A} (\code{n} x \code{d}).}

\item{subset}{Integer vector of coordinates to update.}
}
\value{
Matrix (\code{n} x \code{d}).
}
\description{
For each coordinate \code{j} in \code{subset}, draws
\eqn{\zeta_{\cdot j} \sim TN(\mu_{\cdot j}, 1)} truncated below at
\eqn{(UA)_{\cdot j}} where \eqn{y = 1} and above where \eqn{y = 0}.
}
